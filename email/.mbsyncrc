# mbsyncrc based on
# http://www.ict4g.net/adolfo/notes/2014/12/27/EmacsIMAP.html
# ACCOUNT INFORMATION
IMAPAccount acc1-gmail
# Address to connect to
Host imap.gmail.com
User gregheiman02@gmail.com
PassCmd "gpg2 -q --for-your-eyes-only --no-tty -d ~/.emacs.d/email/.mbsyncpass-acc1.gpg"
AuthMechs LOGIN
SSLType IMAPS
SSLVersions TLSv1.2
CertificateFile /etc/pki/tls/certs/ca-bundle.crt

# THEN WE SPECIFY THE LOCAL AND REMOTE STORAGE
# - THE REMOTE STORAGE IS WHERE WE GET THE MAIL FROM (E.G., THE
#   SPECIFICATION OF AN IMAP ACCOUNT)
# - THE LOCAL STORAGE IS WHERE WE STORE THE EMAIL ON OUR COMPUTER

# REMOTE STORAGE (USE THE IMAP ACCOUNT SPECIFIED ABOVE)
IMAPStore acc1-gmail-remote
Account acc1-gmail

# LOCAL STORAGE (CREATE DIRECTORIES with mkdir -p ~/Maildir/acc1-gmail)
MaildirStore acc1-gmail-local
SubFolders Verbatim
Path ~/Mail/acc1-gmail/
Inbox ~/Mail/acc1-gmail/INBOX

# CONNECTIONS SPECIFY LINKS BETWEEN REMOTE AND LOCAL FOLDERS
#
# CONNECTIONS ARE SPECIFIED USING PATTERNS, WHICH MATCH REMOTE MAIl
# FOLDERS. SOME COMMONLY USED PATTERS INCLUDE:
#
# 1 "*" TO MATCH EVERYTHING
# 2 "!DIR" TO EXCLUDE "DIR"
# 3 "DIR" TO MATCH DIR

Channel acc1-gmail-inbox
Far :acc1-gmail-remote:
Near :acc1-gmail-local:
Patterns "INBOX"
Expunge Both
Create Both
SyncState *

Channel acc1-gmail-trash
Far :acc1-gmail-remote:"[Gmail]/Trash"
Near :acc1-gmail-local:"[acc1].Trash"
Expunge Both
Create Both
SyncState *

Channel acc1-gmail-sent
Far :acc1-gmail-remote:"[Gmail]/Sent Mail"
Near :acc1-gmail-local:"[acc1].Sent Mail"
Expunge Both
Create Both
SyncState *

Channel acc1-gmail-all
Far :acc1-gmail-remote:"[Gmail]/All Mail"
Near :acc1-gmail-local:"[acc1].All Mail"
Expunge Both
Create Both
SyncState *

Channel acc1-gmail-starred
Far :acc1-gmail-remote:"[Gmail]/Starred"
Near :acc1-gmail-local:"[acc1].Starred"
Expunge Both
Create Both
SyncState *

# GROUPS PUT TOGETHER CHANNELS, SO THAT WE CAN INVOKE
# MBSYNC ON A GROUP TO SYNC ALL CHANNELS
#
# FOR INSTANCE: "mbsync gmail" GETS MAIL FROM
# "gmail-inbox", "gmail-sent", and "gmail-trash"
#
Group acc1-gmail
Channel acc1-gmail-inbox
Channel acc1-gmail-sent
Channel acc1-gmail-trash
Channel acc1-gmail-all
Channel acc1-gmail-starred


# ================================================================================


# ACCOUNT INFORMATION
IMAPAccount acc2-gmail
# Address to connect to
Host imap.gmail.com
User treebark1378@gmail.com
PassCmd "gpg2 -q --for-your-eyes-only --no-tty -d ~/.emacs.d/email/.mbsyncpass-acc2.gpg"
AuthMechs LOGIN
SSLType IMAPS
SSLVersions TLSv1.2
CertificateFile /etc/pki/tls/certs/ca-bundle.crt

# THEN WE SPECIFY THE LOCAL AND REMOTE STORAGE
# - THE REMOTE STORAGE IS WHERE WE GET THE MAIL FROM (E.G., THE
#   SPECIFICATION OF AN IMAP ACCOUNT)
# - THE LOCAL STORAGE IS WHERE WE STORE THE EMAIL ON OUR COMPUTER

# REMOTE STORAGE (USE THE IMAP ACCOUNT SPECIFIED ABOVE)
IMAPStore acc2-gmail-remote
Account acc2-gmail

# LOCAL STORAGE (CREATE DIRECTORIES with mkdir -p ~/Maildir/acc2-gmail)
MaildirStore acc2-gmail-local
Subfolders Verbatim
Path ~/Mail/acc2-gmail/
Inbox ~/Mail/acc2-gmail/INBOX

# CONNECTIONS SPECIFY LINKS BETWEEN REMOTE AND LOCAL FOLDERS
#
# CONNECTIONS ARE SPECIFIED USING PATTERNS, WHICH MATCH REMOTE MAIl
# FOLDERS. SOME COMMONLY USED PATTERS INCLUDE:
#
# 1 "*" TO MATCH EVERYTHING
# 2 "!DIR" TO EXCLUDE "DIR"
# 3 "DIR" TO MATCH DIR

Channel acc2-gmail-inbox
Far :acc2-gmail-remote:
Near :acc2-gmail-local:
Patterns "INBOX"
Expunge Both
Create Both
SyncState *

Channel acc2-gmail-trash
Far :acc2-gmail-remote:"[Gmail]/Trash"
Near :acc2-gmail-local:"[acc2].Trash"
Expunge Both
Create Both
SyncState *

Channel acc2-gmail-sent
Far :acc2-gmail-remote:"[Gmail]/Sent Mail"
Near :acc2-gmail-local:"[acc2].Sent Mail"
Expunge Both
Create Both
SyncState *

Channel acc2-gmail-all
Far :acc2-gmail-remote:"[Gmail]/All Mail"
Near :acc2-gmail-local:"[acc2].All Mail"
Expunge Both
Create Both
SyncState *

Channel acc2-gmail-starred
Far :acc2-gmail-remote:"[Gmail]/Starred"
Near :acc2-gmail-local:"[acc2].Stared"
Expunge Both
Create Both
SyncState *

# GROUPS PUT TOGETHER CHANNELS, SO THAT WE CAN INVOKE
# MBSYNC ON A GROUP TO SYNC ALL CHANNELS
#
# FOR INSTANCE: "mbsync gmail" GETS MAIL FROM
# "gmail-inbox", "gmail-sent", and "gmail-trash"
#
Group acc2-gmail
Channel acc2-gmail-inbox
Channel acc2-gmail-sent
Channel acc2-gmail-trash
Channel acc2-gmail-all
Channel acc2-gmail-starredr

# ================================================================================

# ACCOUNT INFORMATION
IMAPAccount outlook-wsu
# Address to connect to
Host outlook.office365.com
Port 993
User w459e964@wichita.edu 
PassCmd "gpg2 -q --for-your-eyes-only --no-tty -d ~/.emacs.d/email/.mbsyncpass-outlookwsu.gpg"
AuthMechs PLAIN
SSLType IMAPS
SSLVersions TLSv1.2
CertificateFile /etc/pki/tls/certs/ca-bundle.crt

# THEN WE SPECIFY THE LOCAL AND REMOTE STORAGE
# - THE REMOTE STORAGE IS WHERE WE GET THE MAIL FROM (E.G., THE
#   SPECIFICATION OF AN IMAP ACCOUNT)
# - THE LOCAL STORAGE IS WHERE WE STORE THE EMAIL ON OUR COMPUTER

# REMOTE STORAGE (USE THE IMAP ACCOUNT SPECIFIED ABOVE)
IMAPStore outlook-wsu-remote 
Account outlook-wsu 

# LOCAL STORAGE (CREATE DIRECTORIES with mkdir -p ~/Maildir/acc2-gmail)
MaildirStore outlook-wsu-local
Subfolders Legacy
Path ~/Mail/outlook-wsu/
Inbox ~/Mail/outlook-wsu/INBOX

# CONNECTIONS SPECIFY LINKS BETWEEN REMOTE AND LOCAL FOLDERS
#
# CONNECTIONS ARE SPECIFIED USING PATTERNS, WHICH MATCH REMOTE MAIl
# FOLDERS. SOME COMMONLY USED PATTERS INCLUDE:
#
# 1 "*" TO MATCH EVERYTHING
# 2 "!DIR" TO EXCLUDE "DIR"
# 3 "DIR" TO MATCH DIR

Channel outlook-wsu-everything
Far :outlook-wsu-remote:
Near :outlook-wsu-local:
Patterns "INBOX" "Other" "Archive" "Drafts" "Deleted Items"
Expunge Both
Create Both
SyncState *

# GROUPS PUT TOGETHER CHANNELS, SO THAT WE CAN INVOKE
# MBSYNC ON A GROUP TO SYNC ALL CHANNELS
#
# FOR INSTANCE: "mbsync gmail" GETS MAIL FROM
# "gmail-inbox", "gmail-sent", and "gmail-trash"
#
Group outlook-wsu 
Channel outlook-wsu-everything
